# Task T-221/T-222/T-223: Better Auth + Neon Configuration (Official JWT Plugin)
# Copy to .env and configure for your environment

# Database Configuration (Neon Serverless PostgreSQL)
# For local development, use Docker PostgreSQL or Neon free tier
# For production, get connection string from Neon console
DATABASE_URL=postgresql://evo_todo_user:evo_todo_password@localhost:5432/evo_todo

# Better Auth Configuration (T-221, Official JWT Plugin with EdDSA/JWKS)
# BETTER_AUTH_URL is the frontend URL where Better Auth service runs
# The backend will automatically derive:
#   - BETTER_AUTH_JWKS_URL = {BETTER_AUTH_URL}/api/auth/jwks
#   - BETTER_AUTH_ISSUER = {BETTER_AUTH_URL}
#   - BETTER_AUTH_AUDIENCE = {BETTER_AUTH_URL}
BETTER_AUTH_URL=http://localhost:3000

# Optional: Override derived URLs if needed
# BETTER_AUTH_JWKS_URL=http://localhost:3000/api/auth/jwks
# BETTER_AUTH_ISSUER=http://localhost:3000
# BETTER_AUTH_AUDIENCE=http://localhost:3000

# JWKS Cache Configuration
# How long to cache JWKS public keys (seconds)
JWKS_CACHE_LIFESPAN=300

# Maximum number of keys to cache before eviction
JWKS_CACHE_MAX_KEYS=16

# CORS Configuration
# Frontend origin for cross-origin requests
CORS_ORIGINS=http://localhost:3000

# Environment
ENVIRONMENT=development
DEBUG=True
